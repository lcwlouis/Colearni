task_type: graph
version: 1
output_format: json
description: User input for batched disambiguation – multiple raw concepts with candidates

Resolve the following concepts. For each, decide MERGE_INTO, LINK_ONLY, or CREATE_NEW.

{batch_items_json}
