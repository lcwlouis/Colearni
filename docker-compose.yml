services:
  db:
    image: pgvector/pgvector:pg16
    restart: unless-stopped
    environment:
      POSTGRES_USER: colearni
      POSTGRES_PASSWORD: colearni
      POSTGRES_DB: colearni
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U colearni -d colearni"]
      interval: 5s
      timeout: 5s
      retries: 10

volumes:
  pgdata:
