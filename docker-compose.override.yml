version: '3.8'

services:
  ai-research-app:
    environment:
      - FLASK_ENV=development
      - DEBUG=true
    volumes:
      - ./src:/app/src  # Enable hot reload
    command: ["python", "-m", "flask", "run", "--host=0.0.0.0", "--debug"]

  searxng:
    environment:
      - SEARXNG_DEBUG=true